const initialState = { clicked: false }

const buttonClickReducer = (state=initialState, action) => {
    switch (action.type) {
        //case 'CLICKED': return state
        default: return state
    }
}

export default buttonClickReducer